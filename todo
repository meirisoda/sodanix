- obsidian submodules check
- blender not recognizing nvidia 
- external monitors defaulting to 60hz 
- nearlyfreespeech
- add second yubikey !!!! # please do this holy fuck
- # TODO restructure config folders:
    - default.nix
        - includes time and locale
        - input methods 
        - bootloader # TODO figure out how to do by host!! 
    - flake.nix:
        - inputs
        - outputs # see hosts
            - ROG
            - XPS
    - system:
        - core 
            - boot # TODO figure out how to do by host!! 
            - security (unsure)
        # - hardware
        #     - bluetooth
        #     - sound 
        # - network
            # - ssh
            # - network manager
        - programs (unsure)
        - nix (unsure)
    - home:
        - packages
            - system
            - utilities
            - 
    - hosts: # TODO check modules
        # - ROG
        #     - hardware configuration
        #      - add hardware-config to flake.nix
        #     # https://github.com/NixOS/nixos-hardware
        #     # make sure to add if host name
        # - XPS
        #     - hardware configuration
        #     - add hardware-config to flake.nix 
        #     # https://github.com/NixOS/nixos-hardware
        #     # make sure to add if host name