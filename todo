- obsidian submodules check
- blender not recognizing nvidia 
- external monitors defaulting to 60hz 
- nearlyfreespeech
- add second yubikey !!!! # please do this holy fuck
- restructure config folders:
    - default.nix
        - includes time and locale
        - input methods 
        - bootloader
    - flake.nix:
        - inputs
        - outputs # see hosts
            - ROG
            - XPS
    - system:
        - core 
            - boot
            - security (unsure)
        - hardware
            - bluetooth
            - sound 
        - network
            - ssh
            - network manager
        - programs (unsure)
        - nix (unsure)
    - home:
        - packages
            - system
            - utilities
            - 
    - hosts:
        - ROG
            - hardware configuration
             - add hardware-config to flake.nix
            # https://github.com/NixOS/nixos-hardware
            # make sure to add if host name
        - XPS
            - hardware configuration
            - add hardware-config to flake.nix
            # https://github.com/NixOS/nixos-hardware
            # make sure to add if host name